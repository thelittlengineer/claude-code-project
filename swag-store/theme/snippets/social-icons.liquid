{%- liquid
  assign networks = 'instagram,tiktok,x,youtube,facebook' | split: ','
  assign has_any = false
  for network in networks
    assign key = 'social_' | append: network | append: '_link'
    if settings[key] != blank
      assign has_any = true
    endif
  endfor
-%}
{%- if has_any -%}
  <ul class="social-icons" role="list">
    {%- for network in networks -%}
      {%- assign key = 'social_' | append: network | append: '_link' -%}
      {%- if settings[key] != blank -%}
        <li>
          <a href="{{ settings[key] }}" class="social-icons__link" target="_blank" rel="noopener" aria-describedby="a11y-new-window-message">
            {% render 'icon', icon: network %}
            <span class="visually-hidden">{{ network | capitalize }}</span>
          </a>
        </li>
      {%- endif -%}
    {%- endfor -%}
  </ul>
{%- endif -%}
